import StartComponent from "../../components/Start/StartComponent";
const startText = "START >>>>";

function Start() {
    return (
        <StartComponent title={startText}/>
    )
};

export default Start;